import React from 'react';
/* SASS used to add background image and height to the header */
const Header = () => {
  return (
    <div
      id="header"
      className="header-bg main-header"
      style={{ marginTop: '50px' }}
    />
  );
};

export default Header;
