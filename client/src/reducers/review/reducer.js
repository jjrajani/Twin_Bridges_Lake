import t from '../../actions/review/types';

export default function(state = {}, action) {
  switch (action.type) {
    // case t.CREATE_REVIEW: {
    //
    // }
    default:
      return state;
  }
}
